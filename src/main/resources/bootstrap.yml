spring:
  application:
    name: customer-service
  profiles:
    active: dev
  cloud:
    config:
      uri: https://bootcamp-nttdata-config-server.azurewebsites.net
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

